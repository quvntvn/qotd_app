package com.quvntvn.qotd_app

import android.content.Context

/**
 * A placeholder for the TranslationManager class.
 * This class is no longer used for translation but is kept to prevent build errors
 * if it is referenced elsewhere.
 */
class TranslationManager(context: Context) {
    /**
     * A placeholder for the translate method.
     * @return the original text.
     */
    fun translate(text: String, lang: String): String {
        return text
    }

    /**
     * A placeholder for the close method.
     */
    fun close() {
        // Nothing to do here.
    }
}
